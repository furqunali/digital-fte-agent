[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "digital-fte-agent"
version = "0.1.0"
description = "Core workflow utilities for a Digital FTE agent"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
